

# План: Галерейный Режим, Перетаскиваемое PiP и Новые Фишки

## Обзор

Добавляю три основных улучшения для видеозвонков:
1. **Галерейный режим (Grid Layout)** - для групповых звонков с 3+ участниками
2. **Перетаскиваемое PiP окно** - свободное перемещение по экрану
3. **Переключатель режимов** - кнопка для смены между Focus/Grid

Плюс рекомендации по дополнительным фишкам в конце плана.

---

## 1. Создание GalleryVideoLayout.tsx

Новый компонент для grid-режима отображения всех участников:

```text
┌─────────────────────────────────────────┐
│  ┌─────────┐ ┌─────────┐ ┌─────────┐    │
│  │ Участн.1│ │ Участн.2│ │ Участн.3│    │
│  └─────────┘ └─────────┘ └─────────┘    │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐    │
│  │ Участн.4│ │ Участн.5│ │    Вы   │    │
│  └─────────┘ └─────────┘ └─────────┘    │
└─────────────────────────────────────────┘
```

**Особенности:**
- Адаптивная сетка (2 столбца для 2-4 участников, 3 столбца для 5+)
- Все видео одинакового размера
- Локальный участник включен в общую сетку с пометкой "Вы"
- Имя участника под каждым видео
- Плавная анимация при добавлении/удалении участников
- Индикатор говорящего (цветная рамка)

---

## 2. Обновление FocusVideoLayout - Draggable PiP

Заменю статичное PiP окно на перетаскиваемое:

```text
┌─────────────────────────────────────────┐
│                                         │
│     Удаленный участник (fullscreen)     │
│                                         │
│                          ┌─────────┐    │
│                          │   Вы   │◄── Можно перетащить
│                          └─────────┘    │   в любой угол
└─────────────────────────────────────────┘
```

**Функционал:**
- Drag & drop мышью/тачем
- Snap к углам экрана (магнитное притягивание)
- Сохранение позиции в sessionStorage
- Анимация bounce при "приземлении"
- Двойной клик = развернуть своё видео на весь экран

---

## 3. Переключатель режимов в нижней панели

Добавлю кнопку между VirtualBackground и Drawing:

```text
[ Камера ] [ Микрофон ] [ Шеринг ] | [ Фоны ] [ 📐 РЕЖИМ ] [ Рисование ] [ Реакции ] ...
```

**Логика переключения:**
- При 2 участниках: Focus режим по умолчанию
- При 3+ участниках: предлагает переключиться на Grid
- Автоматическое переключение на Focus при screen share
- Иконка меняется: `User` (Focus) ↔ `LayoutGrid` (Grid)

---

## 4. Технические Изменения

### Файл: `src/components/GalleryVideoLayout.tsx` (новый)

```typescript
// Пропсы
interface GalleryVideoLayoutProps {
  localParticipant: LocalParticipant | null;
  isCameraEnabled: boolean;
  speakingParticipant?: string; // identity говорящего
}

// Адаптивная сетка
const getGridCols = (count: number) => {
  if (count <= 2) return 'grid-cols-2';
  if (count <= 4) return 'grid-cols-2';
  if (count <= 6) return 'grid-cols-3';
  return 'grid-cols-4'; // max 4 колонки
};

// Каждый тайл
<div className={cn(
  "relative aspect-video rounded-xl overflow-hidden",
  isSpeaking && "ring-2 ring-primary ring-offset-2 ring-offset-background"
)}>
  <VideoTrack ... />
  <div className="absolute bottom-2 left-2 ...">
    {participant.name}
  </div>
</div>
```

### Файл: `src/components/DraggablePiP.tsx` (новый)

```typescript
// Специализированный компонент для PiP видео
interface DraggablePiPProps {
  children: ReactNode;
  storageKey?: string;
  snapToCorners?: boolean;
  onDoubleClick?: () => void;
}

// Corner snap zones
const CORNERS = [
  { id: 'top-left', x: 16, y: 100 },
  { id: 'top-right', x: window.innerWidth - 160, y: 100 },
  { id: 'bottom-left', x: 16, y: window.innerHeight - 180 },
  { id: 'bottom-right', x: window.innerWidth - 160, y: window.innerHeight - 180 },
];

// Snap logic
const snapToNearestCorner = (x: number, y: number) => {
  const SNAP_DISTANCE = 50;
  // ... find nearest corner within distance
};
```

### Файл: `src/components/LiveKitRoom.tsx` (изменения)

```typescript
// Новый стейт
const [layoutMode, setLayoutMode] = useState<'focus' | 'gallery'>('focus');

// Автоматическое переключение
useEffect(() => {
  const remoteCount = participants.filter(p => !p.isLocal).length;
  // При 3+ участниках предложить gallery
  if (remoteCount >= 2 && layoutMode === 'focus') {
    toast.info('Много участников', {
      description: 'Переключитесь на галерею для удобства',
      action: { label: 'Галерея', onClick: () => setLayoutMode('gallery') }
    });
  }
}, [participants.length]);

// Рендер
{layoutMode === 'focus' ? (
  <FocusVideoLayout ... />
) : (
  <GalleryVideoLayout ... />
)}

// Кнопка в нижней панели
<Button onClick={() => setLayoutMode(layoutMode === 'focus' ? 'gallery' : 'focus')}>
  {layoutMode === 'focus' ? <LayoutGrid /> : <User />}
</Button>
```

### Файл: `src/components/FocusVideoLayout.tsx` (изменения)

```typescript
// Заменить статичный PiP на DraggablePiP
import { DraggablePiP } from './DraggablePiP';

// Вместо div с absolute positioning:
<DraggablePiP
  storageKey="local-pip-position"
  snapToCorners={true}
  onDoubleClick={() => {/* swap with main */}}
>
  <VideoTrack ... />
</DraggablePiP>
```

---

## 5. Рекомендуемые Дополнительные Фишки

### A. Индикатор Говорящего (Speaking Indicator)
- Зеленая пульсирующая рамка вокруг видео говорящего
- Audio level визуализация (как бары эквалайзера)
- Автоматический zoom на говорящего в Focus режиме

### B. Пинн участника (Pin Participant)
- Клик правой кнопкой → "Закрепить"
- Закрепленный участник всегда на главном экране в Focus
- Значок 📌 в углу видео

### C. Spotlight Mode
- Ведущий может "подсветить" участника для всех
- Все видят подсвеченного участника на главном экране
- Через Data Channel синхронизация

### D. Быстрые Действия при Hover
```text
┌─────────────────────────┐
│                         │
│     Видео Участника     │
│                         │
│  [🔇 Mute] [📌 Pin] [⛶]│  ← появляется при наведении
└─────────────────────────┘
```

### E. Виртуальный Layout "Webinar"
- 1 большой спикер + лента зрителей снизу
- Для вебинаров и презентаций

### F. Picture-in-Picture (браузерный)
- Нативный PiP браузера для основного видео
- Работает даже при переключении вкладок
- Кнопка "PiP" в углу видео

### G. Клавиатурные Сочетания
- `M` - mute/unmute микрофон
- `V` - вкл/выкл камеру
- `G` - переключить Grid/Focus
- `Space` - push-to-talk
- `H` - поднять руку

### H. Статистика Качества на Видео
- FPS, битрейт, пинг прямо на видео-тайле
- Для отладки проблем с качеством

---

## Порядок Реализации

| Шаг | Описание | Файл(ы) |
|-----|----------|---------|
| 1 | Создать DraggablePiP компонент | `DraggablePiP.tsx` (новый) |
| 2 | Интегрировать в FocusVideoLayout | `FocusVideoLayout.tsx` |
| 3 | Создать GalleryVideoLayout | `GalleryVideoLayout.tsx` (новый) |
| 4 | Добавить переключатель режимов | `LiveKitRoom.tsx` |
| 5 | Добавить индикатор говорящего | Оба layout компонента |
| 6 | Добавить keyboard shortcuts | `LiveKitRoom.tsx` |

---

## Ожидаемый Результат

После реализации:
- **1-2 участника**: Focus режим с перетаскиваемым PiP
- **3+ участников**: Автоматическое предложение Gallery режима
- **Гибкость**: Переключение в любой момент одной кнопкой
- **UX**: Snap-to-corners для PiP, индикаторы говорящего
- **Pro фишки**: Hotkeys, pin, spotlight для продвинутых пользователей

